{
	"index": {
		"title": "Resource Translator",
		"description": "Resource Translator opens machine-translated pull requests for your resource files in any repo, in any format — powered by Azure AI Translator, AWS Translate, or Google Cloud Translation.",
		"socialTitle": "Resource Translator — translate resource files automatically",
		"badge": "v3.0 — Azure, AWS & Google providers, Node 24, repo config",
		"headlinePrefix": "Localize your repo on",
		"headlineAccent": "every commit.",
		"lede": "Resource Translator opens machine-translated pull requests for your resource files using Azure, AWS, or Google — one unified action, one provider per run, official SDKs under the hood.",
		"primaryCta": "Get started",
		"secondaryCta": "Star on GitHub",
		"supports": "Supports",
		"builtForShipping": "Built for shipping",
		"workflowTitle": "Translation as a workflow, not a chore.",
		"workflowBody": "Drop the action into your existing CI, point it at Azure, AWS, or Google, and let your repo speak every language you target.",
		"stats": {
			"audit": "0 npm-audit issues",
			"tests": "240+ tests",
			"node": "Node 20 / 22 / 24",
			"sdk": "Azure · AWS · Google"
		},
		"formatNotes": {
			"resx": "{dotnet} XML",
			"xliff": "XLIFF 2.0",
			"po": "gettext",
			"json": "nested or flat",
			"ini": "{keyValue}",
			"restext": "MUI text"
		},
		"features": {
			"formats": {
				"title": "Six file formats",
				"body": "Round-trips {resx}, {xliff}, {po}, {json}, {ini}, and {restext} with byte-stable formatting."
			},
			"config": {
				"title": "Repo-level config",
				"body": "Drop a {configPath} file for include globs, glossaries, and Custom Translator categories."
			},
			"glossary": {
				"title": "Glossary-aware",
				"body": "Lock brand names and product terms with smart word-boundary regex that handles {cpp}, {dotnet}, and friends."
			},
			"dryRun": {
				"title": "Dry-run + soft-fail",
				"body": "Preview translations without writing files, or surface every error as a warning while you iterate."
			},
			"outputs": {
				"title": "PR-friendly outputs",
				"body": "{summaryTitle}, {summaryDetails}, and {hasNewTranslations} slot directly into create-pull-request."
			},
			"sdk": {
				"title": "Azure, AWS & Google",
				"body": "Pick one provider per run. Each is driven by its official SDK ({azureSdk}, {awsSdk}, {googleSdk}) with automatic retries on {statusCodes} — no hand-rolled HTTP. {esbuild}-bundled, audit-clean."
			}
		},
		"workflowPreview": {
			"eyebrow": "Drop-in workflow",
			"titlePrefix": "Copy. Paste.",
			"titleAccent": "Translate.",
			"body": "Three secrets, four lines, and your repo is multilingual on the next push.",
			"setupLink": "Walk through the full setup"
		},
		"cta": {
			"title": "Ready to localize on autopilot?",
			"body": "One provider key — Azure, AWS, or Google — and one workflow file. Resource Translator handles the rest.",
			"button": "Get started"
		}
	},
	"getting-started": {
		"title": "Getting started",
		"eyebrow": "Setup",
		"heroLede": "Add a workflow file, choose your translation provider — Azure, AWS, or Google — and the action scans the repo for source-language resource files and emits translated siblings.",
		"steps": {
			"provider": {
				"title": "Choose a translation provider",
				"body": "The action works with {azure}, {aws}, and {google} behind one unified API surface. Pick a single provider per run with the {provider} input — the workflow behaves identically regardless of provider; only the credentials differ. See {providersLink} for a full comparison.",
				"azure": "Azure AI Translator",
				"aws": "AWS Translate",
				"google": "Google Cloud Translation",
				"providersLinkLabel": "Translation providers"
			},
			"configure": {
				"title": "Add credentials and a workflow",
				"body": "Store your provider's credentials as GitHub repository secrets, then create {workflowPath}. Select your provider below for its specific setup — the rest of the workflow is identical.",
				"secretsHeading": "Store these as repository secrets",
				"workflowHeading": "Add the workflow",
				"azure": {
					"note": "Azure is the default provider, so {provider} may be omitted. Provision an Azure AI Translator resource in the Azure portal, then store its credentials:",
					"key": "the subscription key for the resource.",
					"endpoint": "the regional endpoint URL.",
					"region": "the resource's Azure region."
				},
				"aws": {
					"note": "Set {provider} to {providerValue}. Prefer OIDC via {configureAwsCredentials} so no long-lived secrets are stored — the step exports {awsRegionEnv} for you. To use static keys instead, store:",
					"accessKeyId": "your AWS access key ID.",
					"secretAccessKey": "your AWS secret access key.",
					"regionNote": "A region is always required — set it with {awsRegion} or the {awsRegionEnv} environment variable."
				},
				"google": {
					"note": "Set {provider} to {providerValue}. Authenticate with either a service-account JSON credential or an API key — supply exactly one:",
					"credentials": "a service-account JSON key (as a JSON string).",
					"apiKey": "or a Google Cloud API key.",
					"projectNote": "An optional {googleProjectId} is inferred from the credential when omitted."
				}
			},
			"resource": {
				"title": "Commit a source resource file",
				"body": "Resource files use the {namingConvention} convention. For example {sourceFile} becomes {frFile}, {deFile}, and so on after a run."
			},
			"output": {
				"title": "Inspect the output",
				"body": "Once the workflow finishes, the action exposes three outputs you can wire into a PR description: {summaryTitle}, {summaryDetails}, and {hasNewTranslations}. It also writes a Markdown summary into the job page via {coreSummary}."
			}
		},
		"tabs": {
			"azure": "Azure",
			"aws": "AWS",
			"google": "Google",
			"ariaLabel": "Choose a translation provider"
		},
		"nextSteps": {
			"title": "Next steps",
			"providers": "Compare every provider and its credentials side by side.",
			"configuration": "Configure repo-level defaults with {configPath}.",
			"recipes": "See recipes for monorepos, custom glossaries, and Custom Translator categories."
		}
	},
	"providers": {
		"title": "Translation providers",
		"eyebrow": "Reference",
		"lede": "Choose one translation provider per run — Azure AI Translator, AWS Translate, or Google Cloud Translation — behind a single, unified API surface. The orchestration stays provider-neutral while credentials and provider-native tuning stay scoped to the selected vendor.",
		"backwardCompatible": "Fully backward compatible: {provider} defaults to {azure}, so existing Azure workflows keep working unchanged — no edits required.",
		"selecting": {
			"title": "Selecting a provider",
			"body": "Set the {provider} input to {azure} (default), {aws}, or {google}, or use a nested provider block for cleaner credentials and provider-native settings. Only one provider is used per action call; the action deterministically delegates to the matching SDK behind a factory."
		},
		"comparison": {
			"title": "Provider comparison",
			"caption": "Translation provider comparison",
			"columns": {
				"provider": "Provider",
				"value": "provider value",
				"sdk": "SDK",
				"credentials": "Credentials"
			},
			"azure": {
				"name": "Azure AI Translator",
				"value": "azure (default)",
				"credentials": "subscriptionKey, endpoint, region (optional)"
			},
			"aws": {
				"name": "AWS Translate",
				"value": "aws",
				"credentials": "OIDC / default chain, or awsAccessKeyId + awsSecretAccessKey; awsRegion"
			},
			"google": {
				"name": "Google Cloud Translation",
				"value": "google",
				"credentials": "googleApiKey OR googleCredentials (service-account JSON)"
			}
		},
		"azure": {
			"title": "Azure AI Translator (default)",
			"body": "The default provider. Provision an Azure AI Translator resource and pass {subscriptionKey}, {endpoint}, and optionally {region}; either as flat inputs or under a nested {azureBlock} block.",
			"docsHeading": "Official Azure docs",
			"docs": {
				"overview": "Azure Translator documentation",
				"translateApi": "Translate API reference",
				"customTranslator": "Custom Translator overview",
				"languages": "Language support"
			}
		},
		"aws": {
			"title": "AWS Translate",
			"body": "Uses {sdk}. Prefer OIDC via {configureAwsCredentials} so no long-lived secrets are stored — the action reads the AWS SDK default credential chain. Alternatively, pass {awsAccessKeyId} and {awsSecretAccessKey} explicitly. A region is always required, via {awsRegion} or the {awsRegionEnv} environment variable. Provider-native settings include {awsFormality}, {awsBrevity}, {awsTerminologyNames}, and {awsParallelDataNames}.",
			"oidcTitle": "OIDC (recommended)",
			"keysNote": "To use static keys instead of OIDC, drop the {configureAwsCredentials} step and pass {awsAccessKeyId} / {awsSecretAccessKey} (via {secrets}) plus {awsRegion}.",
			"docsHeading": "Official AWS docs",
			"docs": {
				"overview": "Amazon Translate overview",
				"translateTextApi": "TranslateText API reference",
				"formality": "Setting formality",
				"brevity": "Using brevity",
				"terminology": "Custom terminology",
				"parallelData": "Parallel data",
				"languages": "Supported languages"
			}
		},
		"google": {
			"title": "Google Cloud Translation",
			"body": "Uses {sdk} (v2). Authenticate with either an API key ({googleApiKey}) or a service-account JSON credential ({googleCredentials}). Supply exactly one; an optional {googleProjectId} is inferred from the credential when omitted. Provider-native settings include {googleModel}, {googleApiEndpoint}, and {googleAutoRetry}.",
			"docsHeading": "Official Google docs",
			"docs": {
				"overview": "Cloud Translation documentation",
				"setup": "Cloud Translation setup",
				"translateText": "Translating text",
				"languages": "Supported languages"
			}
		},
		"mapping": {
			"title": "Intent-specifier mapping",
			"body": "Provider-specific specifiers are mapped where an equivalent exists. Everything else falls back to sensible defaults so behavior stays consistent.",
			"caption": "Intent-specifier mapping across providers",
			"columns": {
				"input": "Input",
				"azure": "Azure",
				"aws": "AWS",
				"google": "Google"
			},
			"supported": "Supported",
			"ignored": "Ignored",
			"rows": {
				"textType": {
					"azure": "textType (plain / html)",
					"aws": "Ignored (no toggle)",
					"google": "format (text / html)"
				},
				"profanityAction": {
					"azure": "Full support",
					"aws": "Marked / Deleted → masking",
					"google": "Ignored"
				},
				"categoryId": {
					"azure": "Custom Translator",
					"aws": "Ignored",
					"google": "Ignored"
				},
				"allowFallback": {
					"azure": "Supported",
					"aws": "Ignored",
					"google": "Ignored"
				},
				"awsFormality": {
					"azure": "Ignored",
					"aws": "Formality setting",
					"google": "Ignored"
				},
				"awsBrevity": {
					"azure": "Ignored",
					"aws": "Brevity mode",
					"google": "Ignored"
				},
				"awsTerminologyNames": {
					"azure": "Ignored",
					"aws": "Custom terminology",
					"google": "Ignored"
				},
				"awsParallelDataNames": {
					"azure": "Ignored",
					"aws": "Parallel data",
					"google": "Ignored"
				},
				"googleModel": {
					"azure": "Ignored",
					"aws": "Ignored",
					"google": "Translation model"
				},
				"googleApiEndpoint": {
					"azure": "Ignored",
					"aws": "Ignored",
					"google": "API endpoint override"
				}
			}
		},
		"locales": {
			"title": "Locale codes differ per provider",
			"body": "Each provider uses its own locale codes (for example Simplified Chinese is {azureCode} on Azure, {awsCode} on AWS, and {googleCode} on Google). Codes pass through as-is and drive the output file names, so choose {toLocales} values your selected provider supports."
		}
	},
	"configuration": {
		"title": "Configuration",
		"eyebrow": "YAML",
		"heroTitle": "Repository configuration",
		"lede": "Express defaults once in the repository config file and let every workflow inherit them. Action inputs always win when both are provided.",
		"intro": "Override the path with the {configPath} input if you need a non-default location.",
		"schemaHeading": "Schema",
		"fieldReference": {
			"heading": "Field reference",
			"caption": "Configuration field reference",
			"columns": {
				"field": "Field",
				"type": "Type",
				"notes": "Notes"
			},
			"fields": {
				"provider": "Either a provider string or a nested provider block ({azureBlock}, {awsBlock}, or {googleBlock}). Prefer nested blocks for provider-specific settings; keep secrets in workflow inputs rather than committed config.",
				"sourceLocale": "Falls back to the {sourceLocale} input if not set. Also forwarded to Translator as {fromLocale} so short strings aren't autodetected.",
				"toLocales": "Either an array or a comma-separated string.",
				"includeExclude": "Glob patterns evaluated against absolute and workspace-relative paths.",
				"glossary": "Post-translation term overrides. Word-boundary aware (handles {cpp}, {dotnet}).",
				"categoryId": "Azure {customTranslatorLink} category — your industry/domain knob (legal, medical, marketing tone, etc.). Sent as {categoryQuery}.",
				"textType": "Defaults to {plain}. Use {html} only if your resource values contain real HTML fragments.",
				"profanityAction": "How to treat profane terms in translated output.",
				"profanityMarker": "Only meaningful when {profanityAction} is {marked}.",
				"allowFallback": "Set to {false} to fail the translation when your {categoryId} has no deployment for a target locale instead of falling back to the general model.",
				"apiVersion": "Translator REST API version. Defaults to {version}.",
				"awsFormality": "AWS Translate formality setting for supported target languages.",
				"awsBrevity": "Set to {true} to enable AWS Translate brevity mode for supported language pairs.",
				"awsCustomization": "AWS Translate custom terminology and parallel data resource names. Either an array or comma-separated string.",
				"googleModel": "Google Cloud Translation model, e.g. {nmt} or {base}.",
				"googleApiEndpoint": "Optional Google Cloud Translation API endpoint override.",
				"googleAutoRetry": "Optional Google Cloud Translation automatic retry toggle. Leave unset to use the Google client default.",
				"maxRetries": "Maximum retry attempts on transient HTTP responses ({statusCodes}). Defaults to {defaultRetries}. Total HTTP calls per request is {totalCalls}.",
				"retryBackoffMs": "Cap (ms) on any single backoff sleep. Defaults to {defaultBackoff}. Azure's {retryAfter} header is honored exactly when present; otherwise jittered exponential backoff is used, capped at this value.",
				"protectPlaceholders": "Defaults to {true}. When on, tokens like {mustache}, {jsVar}, {indexZero}, {numberFormat}, {printf}, {positionalPrintf} and HTML entities are replaced with sentinel tokens before translation and restored afterward. Disable only when source intentionally contains placeholder-shaped literals.",
				"customPlaceholderPatterns": "Extra regex patterns (without delimiters) added to the default placeholder set, e.g. {angleToken} for custom token syntax. Invalid regexes are ignored.",
				"noTranslatePatterns": "Glob patterns matched against parser-level keys (JSON dotted path, RESX {resxName}, PO {poMsgid}, XLIFF unit {xliffId}, INI/restext key). Matching keys are dropped from the request and pass through with their source value preserved.",
				"changeDetection": "Defaults to {smart}. Uses the committed state manifest to translate only changed or missing parser keys. Set to {disabled} or {false} to preserve the legacy always-translate behavior.",
				"statePath": "Path to the deterministic smart change-detection state manifest. Defaults to {defaultPath}."
			}
		},
		"loadingOrder": {
			"heading": "Loading order",
			"steps": {
				"actionInputs": "The action reads explicit inputs from the workflow.",
				"config": "It loads the YAML config from {configPath}.",
				"precedence": "For every shared field, the input wins; otherwise the YAML value is used.",
				"validation": "The merged values are validated. Invalid enums (e.g. {invalidEnum}) cause the action to fail fast with a clear message."
			}
		},
		"tone": {
			"heading": "Tone and industry, in plain terms",
			"body": "The Azure AI Translator v3 API does not have a single tone knob. Two settings combine to give you most of the same control:",
			"industryLabel": "Industry / domain:",
			"industryBody": "use {categoryId} to point at an Azure {customTranslatorLink} category trained on your own corpus — legal, technical, medical, marketing, or anything else. This is the strongest lever for tone, brand voice, and vocabulary.",
			"surfaceLabel": "Surface text:",
			"surfaceBody": "use the {glossaryLink} to lock brand names and product terms verbatim, and use {profanityAction} to scrub profane terms when targeting localization for younger audiences.",
			"glossaryLinkLabel": "glossary"
		}
	},
	"inputs": {
		"title": "Inputs & outputs",
		"eyebrow": "Reference",
		"lede": "Every input is also accepted in the optional repo config file. When both are set, the action input wins.",
		"configurationIntro": "See {configurationLink} for the YAML schema and loading order.",
		"configurationLinkLabel": "Configuration",
		"columns": {
			"name": "Name",
			"default": "Default",
			"description": "Description"
		},
		"defaults": {
			"none": "none",
			"allSupported": "all supported",
			"translatorDefaultTrue": "true (Translator default)"
		},
		"requiredInputs": {
			"heading": "Required inputs",
			"caption": "Required inputs",
			"items": {
				"sourceLocale": "Source locale (e.g. {en}) used to find resource files such as {sourceGlob}. Also forwarded to the provider as {fromLocale}."
			}
		},
		"providerInputs": {
			"heading": "Provider selection & credentials",
			"caption": "Provider selection and per-vendor credentials",
			"intro": "Choose one provider per run with {provider}. It can be a simple provider name or a nested YAML/JSON block with that provider's credentials and native settings. Flat inputs remain supported and override nested values. See the {providersLink} guide for full setup.",
			"items": {
				"provider": "Translation provider: {azure} (default), {aws}, or {google}. Only one provider is used per action call.",
				"subscriptionKey": "Azure AI Translator subscription key. Required for {azure}. Pass via {secrets}.",
				"endpoint": "Azure Translator endpoint URL, e.g. {endpointUrl}. Required for {azure}.",
				"awsAccessKeyId": "AWS access key id. Optional — omit (with {awsSecretAccessKey}) to use the AWS default credential chain / OIDC. Pass via {secrets}.",
				"awsSecretAccessKey": "AWS secret access key. Must be supplied together with {awsAccessKeyId}. Pass via {secrets}.",
				"awsSessionToken": "Optional AWS session token for temporary credentials. Pass via {secrets}.",
				"awsRegion": "AWS region for the Translate service (e.g. {regionExample}). Required for {aws} unless {awsRegionEnv} is set in the environment.",
				"awsFormality": "AWS Translate formality setting: {formal} or {informal}.",
				"awsBrevity": "When {true}, enables AWS Translate brevity mode for supported language pairs.",
				"awsTerminologyNames": "AWS Translate custom terminology resource names. Accepts a JSON array or comma-separated values.",
				"awsParallelDataNames": "AWS Translate parallel data resource names. Accepts a JSON array or comma-separated values.",
				"googleApiKey": "Google Cloud API key. Provide this OR {googleCredentials} for {google}. Pass via {secrets}.",
				"googleCredentials": "Google Cloud service-account key as a JSON string. Provide this OR {googleApiKey} for {google}. Pass via {secrets}.",
				"googleProjectId": "Optional Google Cloud project id. Usually inferred from the service-account credential.",
				"googleModel": "Google Cloud Translation model, e.g. {nmt} or {base}.",
				"googleApiEndpoint": "Optional Google Cloud Translation API endpoint override.",
				"googleAutoRetry": "Optional Google Cloud Translation automatic retry toggle. Leave unset to use the Google client default."
			}
		},
		"optionalInputs": {
			"heading": "Common optional inputs",
			"caption": "Common optional inputs",
			"items": {
				"region": "Translator resource region (omit for global resources).",
				"toLocales": "Locales to translate to. Accepts a JSON array ({jsonArray}) or comma-separated values.",
				"include": "Newline-separated glob patterns; only matching files are translated.",
				"exclude": "Newline-separated glob patterns; matching files are skipped (applied after include).",
				"configPath": "Path to the YAML config file (relative to the workspace root).",
				"changeDetection": "Smart change detection mode. {smart} translates only keys whose source value, target presence, state entry, or translation-affecting settings require it. Use {disabled} or {false} to send every eligible key on every run.",
				"statePath": "Path to the compact deterministic smart change-detection state manifest. Defaults to {defaultPath}; commit it so future runs can audit and reuse unchanged translations.",
				"snapshotOnly": "When {true}, create/update the smart change-detection manifest from existing source and target files without calling a translation provider or writing target resource files. Useful when bootstrapping {statePath} while a provider is unavailable.",
				"dryRun": "When {true}, run translations and emit summaries but do not write files.",
				"failOnError": "When {true}, unexpected errors fail the action; when {false}, errors are warnings."
			}
		},
		"translatorInputs": {
			"heading": "Azure Translator request inputs",
			"intro": "These map directly onto Azure AI Translator v3 query parameters and apply to the azure provider. textType and profanityAction are also mapped to AWS/Google where an equivalent exists (see the providers guide); the rest are Azure-only. Every one is optional; the Translator defaults apply when unset.",
			"caption": "Azure Translator request inputs",
			"items": {
				"categoryId": "Azure {customTranslatorLink} category — the industry/domain/tone knob. Forwarded as {categoryQuery}.",
				"textType": "{plain} or {html}. Use {html} only when your resource values are real HTML fragments.",
				"profanityAction": "{noAction}, {marked}, or {deleted}. Controls how profane terms are emitted in translations.",
				"profanityMarker": "{asterisk} or {tag}. Only meaningful when {profanityAction} is {marked}.",
				"allowFallback": "When {false}, Translator returns an error instead of falling back to the general system if your {categoryId} has no deployment for a target locale.",
				"apiVersion": "Translator REST API version."
			}
		},
		"resilienceInputs": {
			"heading": "Resilience & placeholder protection",
			"intro": "These inputs control how the action behaves when Translator throttles your request and how it shields placeholder tokens like {mustache} or {indexZero} from being mangled during translation.",
			"caption": "Resilience and placeholder protection inputs",
			"items": {
				"maxRetries": "Maximum retry attempts on transient HTTP responses ({statusCodes}). The total number of HTTP calls per request is {totalCalls}.",
				"retryBackoffMs": "Cap (in milliseconds) on any single backoff sleep. The action honors Azure's {retryAfter} response header exactly when present; otherwise it uses jittered exponential backoff capped at this value.",
				"protectPlaceholders": "When {true} (default), placeholders such as {mustache}, {jsVar}, {indexZero}, {numberFormat}, {printf}, {positionalPrintf} and HTML entities are replaced with sentinel tokens before translation and restored afterward. Disable only when your source intentionally contains literal placeholder-shaped text that should be translated.",
				"customPlaceholderPatterns": "Newline-separated regexes (without delimiters) added to the default placeholder set, e.g. {angleToken} for custom token syntax.",
				"noTranslatePatterns": "Newline-separated glob patterns matched against parser-level keys (JSON dotted path, RESX {resxName}, PO {poMsgid}, XLIFF unit {xliffId}, INI/restext key). Matching keys are dropped from the Translator request and pass through with their source value preserved."
			}
		},
		"outputs": {
			"heading": "Outputs",
			"caption": "Outputs",
			"items": {
				"summaryTitle": "Short PR-title-friendly summary, e.g. {summaryExample}.",
				"summaryDetails": "Markdown summary suitable for a PR body or job summary.",
				"hasNewTranslations": "{trueString} when one or more new translations were generated."
			}
		}
	},
	"formats": {
		"title": "File formats",
		"eyebrow": "Reference",
		"heroTitle": "Supported file formats",
		"lede": "Six round-trippable resource formats. The action reads each source-locale file, translates the values, and writes a sibling per target locale.",
		"namingConvention": "All files use the {namingConvention} naming convention.",
		"learnMore": "Learn more",
		"items": {
			"resx": {
				"blurb": "XML-based .NET resource files. Translates the inner text of each {dataValue} node, preserving comments and metadata.",
				"specLabel": "Microsoft Learn — Resources in .resx files"
			},
			"xliff": {
				"blurb": "OASIS XLIFF 2.0 documents. Translates each {source} and writes the result into the corresponding {target}, setting {trgLang} for the locale.",
				"specLabel": "OASIS XLIFF 2.0 specification"
			},
			"po": {
				"blurb": "GNU gettext Portable Object files. Translates each {msgid} / {msgidPlural} into {msgstr} / {msgstrPlural} forms.",
				"specLabel": "GNU gettext — PO file format"
			},
			"json": {
				"blurb": "Arbitrary JSON files. Nested objects are flattened with the {delimiter} delimiter for translation, then re-nested on output. Dotted keys ({dottedKey}) are preserved verbatim.",
				"specLabel": "JSON specification (RFC 8259)"
			},
			"ini": {
				"blurb": "Simple {keyValue} files (Win32-style INI or any flat config). Section headers and comments are preserved.",
				"specLabel": "INI file format on Wikipedia"
			},
			"restext": {
				"blurb": "Microsoft MUI text resources — flat {keyValue} pairs used as a textual companion to {resx}.",
				"specLabel": "Microsoft MUI overview"
			}
		},
		"roundTrip": {
			"heading": "Round-trip guarantees",
			"body": "The test suite verifies byte-for-byte round-tripping for representative fixtures of every format. If a format quirk breaks round-tripping in your codebase, please {issueLink} with a minimal sample.",
			"issueLinkLabel": "open an issue"
		}
	},
	"recipes": {
		"title": "Recipes",
		"eyebrow": "Cookbook",
		"lede": "Common patterns for shipping translations: monorepos, glossaries, custom categories, dry runs, and tone control.",
		"sections": {
			"pullRequest": {
				"heading": "Open a pull request after each run"
			},
			"monorepo": {
				"heading": "Monorepos: scope translation per package"
			},
			"glossary": {
				"heading": "Glossary for brand names and product terms",
				"intro": "Drop a {configPath} into the repo:",
				"afterLabel": "after",
				"body": "Glossary terms are applied {after} translation, so the machine-translated output keeps your preferred branding even when the model swapped it for a synonym."
			},
			"customCategory": {
				"heading": "Custom Translator category (industry / domain)",
				"body": "Train an Azure {customTranslatorLink} category on a corpus that captures your industry vocabulary and tone (legal, medical, marketing, internal-engineering, etc.) and pass its ID via {categoryId}. This is the strongest tone lever Azure offers."
			},
			"tone": {
				"heading": "Tone control + strict fallback",
				"body": "Combine a Custom Translator {categoryId} with {profanityAction} and {allowFallbackFalse} to enforce a consistent voice and fail the workflow if the chosen category does not have a deployment for one of your target locales."
			},
			"dryRun": {
				"heading": "Dry run before turning the workflow on",
				"body": "The action still emits summary outputs and a step summary, but no files are written. Combine with {failOnErrorFalse} to surface every issue as a warning while you iterate."
			}
		}
	},
	"faq": {
		"title": "FAQ",
		"eyebrow": "Help",
		"heroTitle": "Frequently asked questions",
		"lede": "Quick answers to the questions that come up most often when adopting the action.",
		"questions": {
			"azure": {
				"heading": "How does the action talk to Azure?",
				"body": "Through Microsoft's official {azureSdkLink} SDK, layered on {corePipelineLink}. That gets you automatic retries on {statusCodes}, typed request and response models, and authentication header handling for free — everything that used to be a hand-rolled {axios} call. The public action surface (inputs, outputs, file outputs) is unchanged."
			},
			"tone": {
				"heading": "Why are translations sometimes off-brand or off-tone?",
				"body": "Machine translation defaults to a general-purpose model. Three knobs help: a {glossaryLink} for hard term overrides, a {customTranslatorLink} for fine-tuned models trained on your tone and vocabulary, and {profanityAction} to scrub or flag profane terms.",
				"glossaryLinkLabel": "glossary",
				"customTranslatorLinkLabel": "Custom Translator category"
			},
			"duplicateFiles": {
				"heading": "How do I avoid translating the same file twice?",
				"body": "Trigger the workflow on {paths} that match your source-locale files only, e.g. {sourceGlob}. The action also includes its own per-trigger file filter when {githubToken} is set, narrowing work to files in the latest commit."
			},
			"offline": {
				"heading": "Can I run the action offline?",
				"body": "Not exactly — Azure AI Translator is the runtime brain. But you can run with {dryRunTrue} to validate parsing and config without making any HTTP requests for translation."
			},
			"node": {
				"heading": "What Node version does the action run on?",
				"body": "v3 runs on the GitHub Actions {nodeRuntime} runtime. The bundled JavaScript is compatible with Node 20, 22, and 24, which is what CI tests."
			},
			"dist": {
				"heading": "Where does the bundled {distDir} come from?",
				"body": "GitHub JS actions ship their compiled JS in the repository. The {distBuild} workflow rebuilds it after every merge to {mainBranch} if it would drift, and {distCheck} blocks PRs that would land out-of-date output."
			},
			"formats": {
				"heading": "What about other resource formats?",
				"body": "Today: {resx}, {xliff}, {po}, {json}, {ini}, {restext}. {issueLink} if you need another — the parser interface is small (parse → map → apply translations → format) and contributions are welcome.",
				"issueLinkLabel": "Open an issue"
			}
		}
	},
	"translations": {
		"title": "Translations",
		"eyebrow": "Showcase",
		"heroTitle": "Hello in every language",
		"lede": "This page is translated by Resource Translator itself, on every push to main, into every language Azure AI Translator supports.",
		"localeCountLabel": "{count} locales",
		"statusComplete": "{translatedCount} currently rendered — complete!",
		"statusPending": "{translatedCount} currently rendered — {pendingCount} pending the next {workflowName} run.",
		"viewWorkflowRuns": "View workflow runs",
		"filter": "Filter",
		"filterPlaceholder": "Search by language, code, or native name…",
		"source": "Source",
		"machineTranslations": "Machine translations",
		"translationPending": "Translation pending — open a {workflowName} run.",
		"noLocales": "No locales match that filter.",
		"howItWorks": {
			"heading": "How this works",
			"thisActionLabel": "this very action",
			"steps": {
				"source": "The English source file lives at {messagesPath}.",
				"workflow": "Pushes that touch it kick off {workflowLink}, which runs {thisAction} against itself.",
				"fanOut": "The action fans out to every Azure AI Translator-supported locale (configured in {configPath}) and writes a sibling {messagesLocaleJson} per target.",
				"pullRequest": "A pull request is opened with the new translations. Merging it republishes this page with the cards filled in."
			}
		},
		"poweredBy": "Powered by Resource Translator and Azure AI Translator."
	},
	"contributing": {
		"title": "Contributing",
		"eyebrow": "Open source",
		"lede": "Bug reports, feature requests, and pull requests are all welcome. The action is small enough to be friendly to first-time contributors.",
		"localDevelopment": {
			"heading": "Local development"
		},
		"addingParser": {
			"heading": "Adding a parser",
			"steps": {
				"fixture": "Add a fixture under {testDataDir}.",
				"implement": "Implement {parserInterface} ({parse}, {format}, {applyTranslations}, {toTranslatableTextMap}).",
				"register": "Register it in {factoryFile}.",
				"extension": "Add the file extension to {translationFileSchemes} in {finderFile}.",
				"tests": "Write round-trip tests for parse → format and applyTranslations."
			}
		},
		"coverage": {
			"heading": "Coverage thresholds",
			"body": "The Jest config enforces minimum coverage. New code must keep the statements + lines threshold above 78% and functions above 90%. Add fixtures and tests alongside your change."
		},
		"docsSite": {
			"heading": "Documentation site",
			"body": "The {docsDir} folder is an Astro 6 + Tailwind v4 project. Run {npmInstall} there once, then {npmRunDev} for local preview. The {pagesWorkflow} workflow ships docs to GitHub Pages on every merge."
		}
	},
	"changelog": {
		"title": "Changelog",
		"eyebrow": "History",
		"lede": "High-level summary of releases. The canonical, authoritative changelog lives in CHANGELOG.md in the repository.",
		"fullRecord": "See {changelogLink} for the full record.",
		"fullRecordLinkLabel": "CHANGELOG.md",
		"releasesHeading": "Releases",
		"fixedLabel": "Fixed:",
		"releases": {
			"v320SmartChangeDetection": "Smart change detection now defaults to {smart}: a compact provider-neutral state manifest ({stateManifest}) tracks parser keys, source hashes, target hashes, and translation-affecting settings so only changed or missing keys are sent to the selected provider. Use {changeDetection}={disabled} or {false} for legacy always-translate behavior, {statePath} to relocate the manifest, and {snapshotOnly} to bootstrap the manifest from existing target files without calling a provider. The same release adds nested {provider} blocks, AWS/Google-native behavior knobs, narrow-friendly reference cards, and official provider-doc links. Key-level audit rule codes are logged for every decision.",
			"v302": "{label} Translator requests now pin {apiVersion} on the wire for both {translate} and {languages}. The Azure SDK only honors the client-level api-version option when a credential is passed; the action authenticates per request via headers, so that option was silently dropped and requests defaulted to a preview api-version whose {translate} contract rejected the body with HTTP 400 (error code {code}). Completes the fix started in 3.0.1.",
			"v301": "{label} The {translate} request body is now a bare JSON array of {textItem} items instead of a wrapped {inputs} object, matching the v3.0 REST contract. On its own this did not restore translations — see 3.0.2."
		},
		"v31": {
			"labels": {
				"multiVendor": "Multi-vendor:",
				"intentMapping": "Intent-specifier mapping:"
			},
			"items": {
				"multiVendor": "{label} translate with {azure}, {aws}, or {google} behind one unified API surface. Pick a single provider per run with the new {provider} input; the action delegates to the matching SDK behind a factory and behaves identically regardless of vendor. Fully backward compatible — {provider} defaults to {azureValue}, so existing Azure workflows keep working unchanged.",
				"aws": "New AWS Translate provider ({awsSdk}). Authenticate with the AWS default credential chain (recommended: OIDC via {configureAwsCredentials}) or explicit {awsAccessKeyId} / {awsSecretAccessKey}; region via {awsRegion} or {awsRegionEnv}.",
				"google": "New Google Cloud Translation provider ({googleSdk}, v2). Authenticate with a service-account JSON credential ({googleCredentials}) or an API key ({googleApiKey}); optional {googleProjectId}.",
				"inputs": "New inputs: {provider}, {awsAccessKeyId}, {awsSecretAccessKey}, {awsRegion}, {googleApiKey}, {googleCredentials}, {googleProjectId}.",
				"intentMapping": "{label} {textType} maps to Google's {format} and {profanityAction} maps to AWS profanity masking where an equivalent exists. Azure-only specifiers ({categoryId}, {apiVersion}, {profanityMarker}, {allowFallback}) are ignored by other providers.",
				"azureRelaxed": "Azure request inputs ({subscriptionKey}, {endpoint}, {region}) are no longer marked required in {actionYml} so other providers can be selected — still validated at runtime when {provider} is {azureValue}.",
				"docs": "New Translation providers page, provider tabs on Getting started, provider selection on the Inputs page, and a refreshed home page leading with Azure, AWS, and Google."
			}
		},
		"v3": {
			"labels": {
				"breaking": "BREAKING",
				"resilience": "Resilience:",
				"placeholderProtection": "Placeholder protection:",
				"perKeyOptOut": "Per-key opt-out:",
				"docsAccessibility": "Docs accessibility:",
				"inlineCodeStyling": "Inline code styling:"
			},
			"issueLabels": {
				"closes46": "Closes #46.",
				"tightens16": "Tightens #16.",
				"closes35": "Closes #35."
			},
			"items": {
				"runtime": "Bumped to {nodeRuntime} runtime ({breaking}).",
				"newInputs": "New inputs: {include}, {exclude}, {configPath}, {categoryId}, {apiVersion}, {dryRun}, {failOnError}, plus advanced Translator knobs {textType}, {profanityAction}, {profanityMarker}, and {allowFallback}.",
				"sourceLocale": "Source locale is now also forwarded to Translator as {fromLocale} so short strings aren't autodetected.",
				"resilience": "{label} automatic retry on transient HTTP responses ({statusCodes}) honoring Azure's {retryAfter} header — falls back to jittered exponential backoff when absent. Tunable via {maxRetries} and {retryBackoffMs}. {issue}",
				"placeholderProtection": "{label} tokens like {mustache}, {jsVar}, {indexZero}, {numberFormat}, {printf}, {positionalPrintf} and HTML entities are wrapped in sentinels before translation and restored afterward, so Translator stops mangling i18next/Mustache/Handlebars/.NET/printf placeholders. Toggle via {protectPlaceholders}; extend with {customPlaceholderPatterns}. {issue}",
				"perKeyOptOut": "{label} new {noTranslatePatterns} input drops matching keys from the Translator request entirely (preserves source values for brand names, error codes, etc.). Matches glob patterns against parser-level keys (JSON dotted path, RESX {resxName}, PO {poMsgid}, XLIFF unit {xliffId}, INI/restext key). {issue}",
				"repoConfig": "Repo-level config via {configPath}.",
				"glossary": "Glossary support, step summary via {coreSummary}.",
				"tooling": "esbuild bundler, ESLint 9 flat config, Prettier, EditorConfig.",
				"audit": "npm audit clean (0 vulnerabilities).",
				"docsSite": "Astro + Tailwind v4 docs site with view transitions and Expressive Code.",
				"docsAccessibility": "{label} WCAG 2.1 AA pass. Logo glyphs converted to paths so they no longer fail color-contrast against gradient stops, the floating {backToTop} button uses {inert} while hidden (instead of an {ariaHidden} toggle that left it tabbable), {formatsAstro} sections no longer collide with their auto-generated heading IDs, three locale cards now expose BCP-47-valid {lang} attributes ({nya}, {lug}, {run}), duplicate {documentation} landmark labels are disambiguated between sidebar and footer, and every data table on {inputs}/{configuration} ships with a screen-reader caption.",
				"inlineCode": "{label} docs prose {codeElement} now wears the warm/amber theme accent — distinct from the brand→accent palette used for links and headings.",
				"automation": "Dependabot auto-merge, CodeQL scanning, CI matrix."
			}
		}
	}
}
