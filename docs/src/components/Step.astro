---
interface Props {
  number: number;
  title: string;
  /** Tailwind gradient utilities for the numbered badge. */
  accent?: string;
}

const {
  number,
  title,
  // Solid AA-passing brand stop — gradient backgrounds with white text were
  // flagged "incomplete" by axe because the rule can't measure a gradient,
  // and the warm-500 stop in the previous default failed AA on its own
  // (white on warm-500 = 2.56:1). Solid brand-700 passes white at 8.41:1.
  accent = "bg-(--color-brand-700)",
} = Astro.props;
---

<section
  class="step-card my-8 rounded-2xl border border-(--border) bg-(--bg-elevated) p-6 transition-shadow hover:shadow-md"
>
  <div class="flex items-start gap-4">
    <span
      aria-hidden="true"
      class:list={[
        "inline-flex size-12 flex-none items-center justify-center rounded-2xl text-xl font-bold text-white shadow-md shadow-(--color-brand-500)/30",
        accent,
      ]}
    >
      {number}
    </span>
    <div class="min-w-0 flex-1">
      <h2
        id={`step-${number}`}
        class="!mt-0 !mb-3 text-2xl font-bold tracking-tight text-(--fg)"
      >
        <span class="sr-only">Step {number}: </span>
        {title}
      </h2>
      <div class="step-body"><slot /></div>
    </div>
  </div>
</section>

<style>
  /* The slotted content participates in the article's prose-rt styling, but
     trim outer block margins so the body sits flush under the title and
     doesn't leave a gap at the bottom of the card. */
  .step-body > :first-child {
    margin-top: 0;
  }
  .step-body > :last-child {
    margin-bottom: 0;
  }
</style>
